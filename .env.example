# Ollama 주소와 모델 (기본값이면 비워 둬도 됨)
OLLAMA_HOST=http://127.0.0.1:11434
LUCKY_MODEL=exaone3.5:2.4b

# 1로 두면 Ollama가 있어도 모의 답변을 쓴다 (화면 시연용)
LUCKY_STUB=0

# 노트를 저장할 폴더 (비우면 프로젝트 안의 vault)
# VAULT_DIR=C:\luckwiki\vault

# 노션 업로드를 쓸 때만 필요 (없으면 '내보내기 버튼'만 동작)
NOTION_TOKEN=
NOTION_DB_ID=
